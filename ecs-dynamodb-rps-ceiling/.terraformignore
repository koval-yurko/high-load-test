# Upload root for HCP Terraform CLI-driven runs is this directory, not terraform/,
# because the workspace's working directory is "terraform". That is what makes
# ${path.module}/../grafana/* resolve in a remote run -- see terraform/collector.tf.
# Everything below is irrelevant to a plan and must not be shipped to HCP.
node_modules/
test/
src/
k6/
scripts/
*.md
*.html
Dockerfile
docker-compose.test.yml
.dockerignore
package.json
package-lock.json
terraform/.terraform/
terraform/tfplan*
